@model Trackii.Models.Admin.Location.LocationListVm

@{
    ViewData["Title"] = "Localidades";
}

<div class="card">

    <div class="page-header">
        <div>
            <h2>Localidades</h2>
            <p class="subtitle">Administración de localidades y estaciones</p>
        </div>

        <a href="/Admin/Location/Create" class="btn btn-primary">
            <i class="fa-solid fa-plus"></i>
            Nueva Localidad
        </a>
    </div>

    <form method="get" action="/Admin/Location" class="filters">
        <input name="search" value="@Model.Search" placeholder="Buscar..." />

        <label class="checkbox">
            <input type="checkbox"
                   name="showInactive"
                   value="true"
                   @(Model.ShowInactive ? "checked" : "") />
            Ver inactivos
        </label>

        <button type="submit" class="btn btn-secondary">Filtrar</button>
    </form>

    @if (TempData["Error"] != null)
    {
        <div class="alert error">
            <i class="fa-solid fa-circle-exclamation"></i>
            @TempData["Error"]
        </div>
    }

    <div class="table-wrapper">
        <table class="table">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Estado</th>
                    <th class="text-right">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @if (!Model.Items.Any())
                {
                    <tr>
                        <td colspan="3" class="empty">No hay localidades registradas</td>
                    </tr>
                }
                else
                {
                    @foreach (var l in Model.Items)
                    {
                        <tr>
                            <td>@l.Name</td>
                            <td>
                                @if (l.Active)
                                {
                                    <span class="badge success">Activa</span>
                                }
                                else
                                {
                                    <span class="badge danger">Inactiva</span>
                                }
                            </td>
                            <td class="text-right">
                                <a href="/Admin/Location/Edit/@l.Id" class="btn-icon" title="Editar">
                                    <i class="fa-solid fa-pen"></i>
                                </a>

                                <form method="post" action="/Admin/Location/Toggle" class="inline-form">
                                    @Html.AntiForgeryToken()
                                    <input type="hidden" name="id" value="@l.Id" />
                                    <input type="hidden" name="active" value="@(l.Active ? 0 : 1)" />
                                    <button type="submit" class="btn-chip @(l.Active ? "danger" : "success")">
                                        @(l.Active ? "Desactivar" : "Activar")
                                    </button>
                                </form>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>

    @if (Model.TotalPages > 1)
    {
        <div class="pager">
            @for (int i = 1; i <= Model.TotalPages; i++)
            {
                if (i == Model.Page)
                {
                    <span class="page current">@i</span>
                }
                else
                {
                    <a class="page"
                       href="/Admin/Location?page=@i&search=@Model.Search&showInactive=@(Model.ShowInactive ? "true" : "")">
                        @i
                    </a>
                }
            }
        </div>
    }
    else
    {
        <div class="pagination">
            Página @Model.Page de @Model.TotalPages
        </div>
    }
</div>
